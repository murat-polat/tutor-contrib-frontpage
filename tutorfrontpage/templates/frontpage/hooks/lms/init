export DJANGO_SETTINGS_MODULE=lms.envs.tutor.production

./manage.py lms shell -c \
  "from django.contrib.auth import get_user_model;"

./manage.py lms manage_user solo solo@test.com --staff --superuser 